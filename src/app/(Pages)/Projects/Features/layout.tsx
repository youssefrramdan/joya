import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Features Projects | Joya Properties",
};

export default function FeaturesProjectsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
